<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamadgirikisan">
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".previousWinners"></activity>
        <activity android:name=".quizHome" />
        <activity android:name=".labelHistory" />
        <activity android:name=".redmptionHome" />
        <activity
            android:name=".ui.login.redempsignupActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.login.redemptionLoginActivity"
            android:label="@string/title_activity_redemption_login" />
        <activity
            android:name=".ui.login.quizLoginActivity"
            android:label="@string/title_activity_quiz_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>